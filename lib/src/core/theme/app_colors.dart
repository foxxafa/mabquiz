import 'package:flutter/material.dart';

class AppColors {
  // Modern koyu tema renk paleti
  static const Color primary = Color(0xFF4F9CF9);      // Modern mavi
  static const Color primaryLight = Color(0xFF6BB6FF);
  static const Color primaryDark = Color(0xFF2E5EAA);

  static const Color secondary = Color(0xFF7C3AED);    // Mor
  static const Color accent = Color(0xFFFF6B6B);       // Kırmızı-pembe

  // Koyu tema arka planlar
  static const Color background = Color(0xFF121212);   // Ana arka plan
  static const Color surface = Color(0xFF1E1E1E);      // Kart arka planı
  static const Color surfaceVariant = Color(0xFF2A2A2A); // İkincil yüzey
  static const Color border = Color(0xFF404040);       // Kenarlık

  // Navigation bar colors
  static const Color navBarBackground = Color(0xFF1E1E1E);
  static const Color navBarBackgroundLight = Color(0xFF2A2A2A);
  static const Color navBarShadow = Color(0xFF0A0A0A);

  static const Color error = Color(0xFFFF4B4B);
  static const Color success = Color(0xFF58CC02);
  static const Color warning = Color(0xFFFF9600);

  // Koyu tema metinler
  static const Color textPrimary = Color(0xFFFFFFFF);    // Beyaz
  static const Color textSecondary = Color(0xFFB3B3B3);  // Açık gri
  static const Color textLight = Color(0xFF808080);      // Orta gri

  // Soru türleri için koyu tema uyumlu renkler
  static const Color multipleChoice = Color(0xFF4F9CF9);  // Mavi
  static const Color trueFalse = Color(0xFF58CC02);       // Yeşil
  static const Color fillBlank = Color(0xFFFF9600);       // Turuncu
  static const Color matching = Color(0xFF9013FE);        // Mor

  // Progress renkler (koyu tema)
  static const Color progressBackground = Color(0xFF404040);
  static const Color progressFill = Color(0xFF4F9CF9);

  // Card shadows (koyu tema için)
  static const Color shadowLight = Color(0x33000000);
  static const Color shadowMedium = Color(0x66000000);
  static const Color shadowDark = Color(0x80000000);
}
